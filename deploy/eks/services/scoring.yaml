apiVersion: apps/v1
kind: Deployment
metadata:
  name: scoring
  namespace: idfc
  labels: { app: scoring, tier: capability, platform: idfc-integration }
spec:
  replicas: 2
  selector:
    matchLabels: { app: scoring }
  template:
    metadata:
      labels: { app: scoring, tier: capability }
    spec:
      containers:
        - name: scoring
          image: idfc/scoring:0.1.0-SNAPSHOT   # over/registry set via kustomize images:
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 8093 }
          env:
            - { name: SPRING_PROFILES_ACTIVE, value: eks }
            - { name: SERVER_PORT, value: "8093" }
          envFrom:
            - configMapRef: { name: idfc-platform-config }
            - secretRef: { name: idfc-platform-secrets }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8093 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8093 }
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits:   { cpu: "1",    memory: "1Gi"  }
---
apiVersion: v1
kind: Service
metadata:
  name: scoring
  namespace: idfc
  labels: { app: scoring }
spec:
  type: ClusterIP
  selector: { app: scoring }
  ports:
    - { name: http, port: 80, targetPort: 8093 }
