apiVersion: apps/v1
kind: Deployment
metadata:
  name: bureau
  namespace: idfc
  labels: { app: bureau, tier: capability, platform: idfc-integration }
spec:
  replicas: 2
  selector:
    matchLabels: { app: bureau }
  template:
    metadata:
      labels: { app: bureau, tier: capability }
    spec:
      containers:
        - name: bureau
          image: idfc/bureau:0.1.0-SNAPSHOT   # over/registry set via kustomize images:
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 8092 }
          env:
            - { name: SPRING_PROFILES_ACTIVE, value: eks }
            - { name: SERVER_PORT, value: "8092" }
          envFrom:
            - configMapRef: { name: idfc-platform-config }
            - secretRef: { name: idfc-platform-secrets }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8092 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8092 }
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits:   { cpu: "1",    memory: "1Gi"  }
---
apiVersion: v1
kind: Service
metadata:
  name: bureau
  namespace: idfc
  labels: { app: bureau }
spec:
  type: ClusterIP
  selector: { app: bureau }
  ports:
    - { name: http, port: 80, targetPort: 8092 }
