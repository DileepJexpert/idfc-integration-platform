apiVersion: apps/v1
kind: Deployment
metadata:
  name: origination-journey
  namespace: idfc
  labels: { app: origination-journey, tier: engine, platform: idfc-integration }
spec:
  replicas: 2
  selector:
    matchLabels: { app: origination-journey }
  template:
    metadata:
      labels: { app: origination-journey, tier: engine }
    spec:
      containers:
        - name: origination-journey
          image: idfc/origination-journey:0.1.0-SNAPSHOT   # over/registry set via kustomize images:
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 8082 }
          env:
            - { name: SPRING_PROFILES_ACTIVE, value: eks }
            - { name: SERVER_PORT, value: "8082" }
          envFrom:
            - configMapRef: { name: idfc-platform-config }
            - secretRef: { name: idfc-platform-secrets }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8082 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8082 }
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits:   { cpu: "1",    memory: "1Gi"  }
---
apiVersion: v1
kind: Service
metadata:
  name: origination-journey
  namespace: idfc
  labels: { app: origination-journey }
spec:
  type: ClusterIP
  selector: { app: origination-journey }
  ports:
    - { name: http, port: 80, targetPort: 8082 }
