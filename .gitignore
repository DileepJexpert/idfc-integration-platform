.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
*.log
.idea/
*.iml
.vscode/
# Anchor IDE output dirs to the repo root so they don't match source packages
# like `adapter/out/` (an un-anchored `out/` silently excluded every out-adapter).
/out/
/bin/
