package com.idfcfirstbank.integration.capabilities.bureau.domain.model;

/** Why the pull is happening — audited and tied to consent (DPDP). */
public enum Purpose {
    ELIGIBILITY,
    LIMIT_ENHANCEMENT,
    UNDERWRITING
}
