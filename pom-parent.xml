<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <!--
      Enterprise parent/BOM for both Maven and Gradle consumers.

      Gradle services consume it with:
      implementation(platform("com.idfcfirstbank:boot-parent:2.0.1"))

      Do not overwrite 2.0.0 in Artifactory. Publish this as a new immutable version.
    -->
    <groupId>com.idfcfirstbank</groupId>
    <artifactId>boot-parent</artifactId>
    <version>2.0.1</version>
    <packaging>pom</packaging>

    <name>IDFC FIRST Bank Java 25 / Spring Boot 4 Parent</name>
    <description>
        IDFC enterprise dependency platform and Maven build baseline for Java 25 and Spring Boot 4.1.
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <java.version>25</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>

        <spring-boot.version>4.1.0</spring-boot.version>
        <!-- Spring Boot 4.1 manages OpenTelemetry SDK/API 1.62.0. -->
        <opentelemetry.instrumentation.version>2.28.1</opentelemetry.instrumentation.version>
        <opentelemetry.instrumentation.alpha.version>2.28.1-alpha</opentelemetry.instrumentation.alpha.version>

        <resilience4j.version>2.4.0</resilience4j.version>
        <spring-data-aerospike.version>6.2.0</spring-data-aerospike.version>
        <aerospike-client.version>10.4.0</aerospike-client.version>
        <springdoc.version>3.0.3</springdoc.version>

        <lombok.version>1.18.46</lombok.version>
        <jacoco.version>0.8.15</jacoco.version>
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.4</maven-surefire-plugin.version>
        <maven-failsafe-plugin.version>3.5.4</maven-failsafe-plugin.version>

        <wiremock.version>3.13.2</wiremock.version>
        <pact.version>4.7.0</pact.version>
        <gatling.version>3.15.0</gatling.version>
        <logstash-logback-encoder.version>7.4</logstash-logback-encoder.version>

        <!-- Existing utility versions retained from the supplied parent. -->
        <json.version>20250107</json.version>
        <velocity.version>2.4.1</velocity.version>
        <commons-csv.version>1.14.0</commons-csv.version>
        <jsch.version>0.2.24</jsch.version>
        <commons-beanutils.version>1.11.0</commons-beanutils.version>
        <commons-validator.version>1.9.0</commons-validator.version>
        <commons-collections.version>3.2.2.redhat-2</commons-collections.version>

        <!--
          IDFC SECURITY OVERRIDES

          Replace the placeholders below with the real Checkmarx/SCA finding IDs,
          owners, approval dates and review dates before publishing.

          Keep an override only while the Spring Boot-managed version does not
          satisfy the approved security finding.
        -->
        <tomcat.version>11.0.23</tomcat.version>
        <netty.version>4.2.16.Final</netty.version>
        <logback.version>1.5.35</logback.version>
        <commons-lang3.version>3.18.0</commons-lang3.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Core framework dependency constraints. -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- OpenTelemetry instrumentation 2.28.1 aligns with SDK/API 1.62.0. -->
            <dependency>
                <groupId>io.opentelemetry.instrumentation</groupId>
                <artifactId>opentelemetry-instrumentation-bom</artifactId>
                <version>${opentelemetry.instrumentation.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Keep only while existing IDFC tracing libraries use alpha instrumentation modules. -->
            <dependency>
                <groupId>io.opentelemetry.instrumentation</groupId>
                <artifactId>opentelemetry-instrumentation-bom-alpha</artifactId>
                <version>${opentelemetry.instrumentation.alpha.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-bom</artifactId>
                <version>${resilience4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
              IDFC SECURITY OVERRIDE
              Finding ID : CHECKMARX-TOMCAT-<replace>
              Component  : org.apache.tomcat.embed
              Owner      : Java Platform Team
              Review by  : <YYYY-MM-DD>
            -->
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-core</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-el</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-websocket</artifactId>
                <version>${tomcat.version}</version>
            </dependency>

            <!--
              IDFC SECURITY OVERRIDE
              Finding ID : CHECKMARX-NETTY-<replace>
              Component  : io.netty
              Owner      : Java Platform Team
              Review by  : <YYYY-MM-DD>
            -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--
              IDFC SECURITY OVERRIDE
              Finding ID : CHECKMARX-LOGBACK-<replace>
              Owner      : Java Platform Team
              Review by  : <YYYY-MM-DD>
            -->
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-core</artifactId>
                <version>${logback.version}</version>
            </dependency>

            <!--
              Kafka is intentionally not overridden here. Spring Boot 4.1 controls
              the Spring Kafka/Kafka client combination. Add an override only after
              MSK compatibility testing and an approved security finding.
            -->

            <!-- Aerospike versions compatible with the Spring Boot 4 generation. -->
            <dependency>
                <groupId>com.aerospike</groupId>
                <artifactId>spring-data-aerospike</artifactId>
                <version>${spring-data-aerospike.version}</version>
            </dependency>
            <dependency>
                <groupId>com.aerospike</groupId>
                <artifactId>aerospike-client-jdk8</artifactId>
                <version>${aerospike-client.version}</version>
            </dependency>

            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback-encoder.version}</version>
            </dependency>

            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>

            <!-- Test and contract-test dependencies. -->
            <dependency>
                <groupId>org.wiremock</groupId>
                <artifactId>wiremock-standalone</artifactId>
                <version>${wiremock.version}</version>
            </dependency>
            <dependency>
                <groupId>au.com.dius.pact.provider</groupId>
                <artifactId>junit5spring</artifactId>
                <version>${pact.version}</version>
            </dependency>
            <dependency>
                <groupId>au.com.dius.pact.consumer</groupId>
                <artifactId>junit5</artifactId>
                <version>${pact.version}</version>
            </dependency>
            <dependency>
                <groupId>io.gatling</groupId>
                <artifactId>gatling-core-java</artifactId>
                <version>${gatling.version}</version>
            </dependency>
            <dependency>
                <groupId>io.gatling</groupId>
                <artifactId>gatling-http-java</artifactId>
                <version>${gatling.version}</version>
            </dependency>

            <!-- Existing general utility constraints. -->
            <dependency>
                <groupId>org.json</groupId>
                <artifactId>json</artifactId>
                <version>${json.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-csv</artifactId>
                <version>${commons-csv.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.mwiede</groupId>
                <artifactId>jsch</artifactId>
                <version>${jsch.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-beanutils</groupId>
                <artifactId>commons-beanutils</artifactId>
                <version>${commons-beanutils.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-validator</groupId>
                <artifactId>commons-validator</artifactId>
                <version>${commons-validator.version}</version>
                <exclusions>
                    <exclusion>
                        <groupId>commons-collections</groupId>
                        <artifactId>commons-collections</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>commons-collections</groupId>
                <artifactId>commons-collections</artifactId>
                <version>${commons-collections.version}</version>
            </dependency>

            <!-- IDFC internal libraries retained from the supplied parent/service. -->
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>common-libraries</artifactId>
                <version>3.0.4</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>middleware-tracing-utility</artifactId>
                <version>1.1.2</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>idfc-http-client-utility</artifactId>
                <version>1.6.5</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>jmi-logging-utility</artifactId>
                <version>1.3.6</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>jmi-masking-utility</artifactId>
                <version>1.0.3</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>jmi-monitoring-utility</artifactId>
                <version>1.0.3</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>idfc-commshub-utility</artifactId>
                <version>1.1.4</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>tracing-middleware</artifactId>
                <version>2.0.33</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>db-utils</artifactId>
                <version>2.0.12</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>oauth-middleware</artifactId>
                <version>2.0.25</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>optimus-utils</artifactId>
                <version>2.0.28</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>jmi-crypto-utility</artifactId>
                <version>1.2.4</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>jmi-sfdc-connector</artifactId>
                <version>1.0.1</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>http-client-utils</artifactId>
                <version>2.0.27</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>db-credentials-refresh-utility</artifactId>
                <version>1.0.3</version>
            </dependency>
            <dependency>
                <groupId>com.idfcfirstbank</groupId>
                <artifactId>aerospike-mule-connector</artifactId>
                <version>1.0.0</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>true</parameters>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven-failsafe-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <distributionManagement>
        <repository>
            <id>idfc-artifactory-releases</id>
            <name>IDFC Artifactory Maven Releases</name>
            <url>https://artifactory.idfcfirstbank.com:443/artifactory/api-boot-maven-local</url>
        </repository>
        <snapshotRepository>
            <id>idfc-artifactory-snapshots</id>
            <name>IDFC Artifactory Maven Snapshots</name>
            <url>https://artifactory.idfcfirstbank.com:443/artifactory/api-boot-maven-local</url>
        </snapshotRepository>
    </distributionManagement>

</project>
