package com.idfcfirstbank.integration.shared.domain;

/**
 * Marker / module anchor for the {@code com.idfcfirstbank.integration.shared.domain} library. Canonical domain types shared across modules.
 * Slice 1 keeps this minimal; types land here as later slices need them.
 */
public final class SharedDomain {
    private SharedDomain() {
    }
}
