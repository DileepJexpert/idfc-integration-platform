package in.idfc.integration.shared.domain;

/**
 * Marker / module anchor for the {@code in.idfc.integration.shared.domain} library. Canonical domain types shared across modules.
 * Slice 1 keeps this minimal; types land here as later slices need them.
 */
public final class SharedDomain {
    private SharedDomain() {
    }
}
