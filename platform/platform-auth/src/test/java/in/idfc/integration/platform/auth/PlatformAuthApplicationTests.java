package in.idfc.integration.platform.auth;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Placeholder test for the Slice 1 stub. Kept framework-free so the stub builds
 * fast and deterministically; the real module gets behavioural tests later.
 */
class PlatformAuthApplicationTests {
    @Test
    void stubModuleIsPresent() {
        assertThat(PlatformAuthApplication.class.getPackageName()).isEqualTo("in.idfc.integration.platform.auth");
    }
}
