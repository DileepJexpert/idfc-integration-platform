package in.idfc.integration.orchestration.originationjourney;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Placeholder test for the Slice 1 stub. Kept framework-free so the stub builds
 * fast and deterministically; the real module gets behavioural tests later.
 */
class OriginationJourneyApplicationTests {
    @Test
    void stubModuleIsPresent() {
        assertThat(OriginationJourneyApplication.class.getPackageName()).isEqualTo("in.idfc.integration.orchestration.originationjourney");
    }
}
